# 作業報告書

## 2026-08-29 / 疑似サイトプレビュー改善

### 目的

STEP 2のレイアウト見本が抽象的なワイヤーフレームで、実際のWebサイトとしての完成像を掴みにくかったため、選んだ配色を反映した実サイト風の疑似表示へ改善する。

### 変更した内容

- `css/site-preview.css` を新規追加。
- STEP 2の24レイアウトを、ロゴ・ナビ・見出し・説明・ボタン・カード・表示枠などを持つ疑似サイト風表示へ変更。
- 検索欄・表・タブ・サイドナビ・タイムライン等も、それぞれ実UIに近い見た目へ変更。
- STEP 1の3色をそのままプレビューへ反映。
- ベース75% / 主アクセント20% / 少量アクセント5%の主従を維持。
- 背景グラデーションは追加していない。
- `標準 / 余白広め / 情報多め / スマホ重視` の切り替えを見た目にも反映。
- スマホ重視は疑似スマホ幅のビューポートを表示。
- `layouts.html` の説明文を疑似サイト表示に合わせて修正。
- `result.html` の最終プレビューにも同じ表示を反映。
- README / CHANGELOGを現仕様へ更新。

### 守った仕様

- レイアウトデータ24件は変更していない。
- 具体的なサイト名称・業種・題材は追加していない。
- `BRAND` / `Main heading` / `Card title` 等の汎用ダミー文言だけを使用。
- 完成プロンプトの構造指定は変更していない。
- 配色データ100件は変更していない。
- sticky目次や常駐ドックは追加していない。
- 重い常時アニメーションは追加していない。

### 変更したファイル

- `css/site-preview.css` 新規
- `layouts.html`
- `result.html`
- `README.md`
- `CHANGELOG.md`
- `docs/作業報告書.md`

### 確認できたこと

- 新規CSS参照パスがGitHub Pagesの相対パス構成と一致している。
- JavaScriptやJSONのデータ構造を変更していないため、選択保存とプロンプト生成仕様は維持。
- 疑似表示内でグラデーションを使用していない。
- 具体用途を示す文言を疑似サイトへ追加していない。

### 未確認

- 24レイアウト × 4バリエーションの全96組み合わせを実ブラウザで目視確認。
- 実機スマートフォンでの全プレビュー表示。
- GitHub Pages反映後のFirefox / Chromium双方での最終視覚確認。

未確認項目は確認済み扱いにしない。

---

## 2026-08-29 / v0.3.0

### 目的

DesignShelfだけを見て継ぎ足すのではなく、同じGitHubアカウント内の他プロジェクトを比較し、日常利用に有効な設計だけを横展開する。

参考確認:
- AP Study Notes
- LyricTube
- ASMRTube
- VReview
- Lineup Lab

### 追加・改善

- `js/shell.js` / `css/shell.css` に共通UIを分離。
- Version `v0.3.0` / BUILD `20260829-1` を表示。
- コンパクト表示。
- 動きを減らす設定と `prefers-reduced-motion`。
- OSテーマを初回テーマへ反映。
- safe-area対応。
- `/` / `R` / `T` / `?` / `Esc` ショートカット。
- ヘルプ。
- データ診断。
- 0件時の絞り込み解除 / ランダム復帰導線。
- 最近選んだ配色・骨格を最大6件。
- ARIA選択状態の改善。
- カラーコードコピー時の短い視覚フィードバック。
- モバイルヘッダーを2段+横スクロール可能にし、sticky目次は再導入しない。
- 配色100件を10色タイプのJSONへ10件ずつ分割し、`data/palettes.json` をマニフェスト化。
- `tests/data-integrity.html` を追加。
- GitHub Actionsを更新。

### 確認済み

- 配色100件。
- レイアウト24件。
- ID重複なし。
- wireframe重複なし。
- JSON構文正常。
- `node scripts/validate-data.mjs` PASS（ローカル作業版）。
- JavaScript構文チェックPASS（ローカル作業版）。
- CSSにグラデーション・`position: sticky` がないことを確認。

### 未確認

- 実GitHub Pages URLでSTEP 1 → STEP 2 → STEP 3の全操作。
- 実機スマートフォンでの視覚確認。

未確認項目は確認済み扱いにしない。
